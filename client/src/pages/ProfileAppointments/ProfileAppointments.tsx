import { FC } from "react";
import styles from "./ProfileAppointments.module.css";

export const ProfileAppointments: FC = () => (
	<div className={styles.wrapper}>
		<h1>All Appointments</h1>
	</div>
);